import { NgDataTableColumnDirective } from './ng-data-table-column.directive';

describe('NgDataTableColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDataTableColumnDirective();
    expect(directive).toBeTruthy();
  });
});
