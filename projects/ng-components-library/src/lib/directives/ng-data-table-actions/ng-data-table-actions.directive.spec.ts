import { NgDataTableActionsDirective } from './ng-data-table-actions.directive';

describe('NgDataTableActionsDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDataTableActionsDirective();
    expect(directive).toBeTruthy();
  });
});
